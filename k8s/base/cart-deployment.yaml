apiVersion: v1
kind: ConfigMap
metadata:
  name: cart-config
  namespace: croc-shop-cart
data:
  PORT: "3003"
  REDIS_HOST: redis.croc-shop-data.svc.cluster.local
  REDIS_PORT: "6379"
---
apiVersion: v1
kind: Secret
metadata:
  name: cart-secret
  namespace: croc-shop-cart
type: Opaque
stringData:
  JWT_SECRET: change-this-in-production-use-k8s-secrets
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: croc-shop-cart
  labels:
    app: cart
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart
  template:
    metadata:
      labels:
        app: cart
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: cart
        image: cjdaigle2/croc-shop-cart:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        envFrom:
        - configMapRef:
            name: cart-config
        env:
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: cart-secret
              key: JWT_SECRET
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3003
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: croc-shop-cart
  labels:
    app: cart
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: cart
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cart-hpa
  namespace: croc-shop-cart
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cart
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
