apiVersion: v1
kind: ConfigMap
metadata:
  name: user-config
  namespace: crock-shop-user
data:
  PORT: "3002"
---
apiVersion: v1
kind: Secret
metadata:
  name: user-secret
  namespace: crock-shop-user
type: Opaque
stringData:
  JWT_SECRET: change-this-in-production-use-k8s-secrets
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
  namespace: crock-shop-user
  labels:
    app: user
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3002"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: user
        image: cjdaigle2/crock-shop:user
        imagePullPolicy: Always
        ports:
        - containerPort: 3002
          name: http
        envFrom:
        - configMapRef:
            name: user-config
        env:
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: user-secret
              key: JWT_SECRET
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: user
  namespace: crock-shop-user
  labels:
    app: user
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: user
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-hpa
  namespace: crock-shop-user
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
